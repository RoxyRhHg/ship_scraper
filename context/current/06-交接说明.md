# 06 — 交接说明

> 最后更新：2026-05-05

## 当前状态（3句话）

ship_scraper_v10 代码已修复（4处bug），待端到端验证。未标注数据集（294 seed + 1656 hard）已整理到 `ship_labeling_new/`，镜像 v2 目录结构，随时可以开始标注。v10 此前 353 张接受的图片全部与 v9.5 SHA1 重复，净新增为 0。

## 立即下一步

**运行 scraper v10 10分钟烟雾测试：**
```bash
cd e:/guangdianbishe/ship_scraper_v10
python scraper_v10.py --target 200 --time-limit 600
```

观察输出中是否出现 `[MQ]` 和 `[Wiki]` 的候选发现日志。检查 `output/reports/accepted.csv` 是否有新图片（与 v9.5 的 accepted.csv SHA1 对比）。

## 不要重复做的事

1. **不要回退系统代理** — 直连池保持 `trust_env=True`，中国网络环境需要代理访问国际站点
2. **不要重新诊断 navsource** — 已确认 DNS 解析失败，该源不可用
3. **不要重新分类未标注数据** — `ship_labeling_new/` 已完成 seed/hard 分类
4. **不要改 per_page_limit** — 当前值 3 合理（navysite.de 某页有 4 张被截断是预期行为）
5. **不要用 `--proxy ""` 直接跑** — 用 `--proxy auto`（默认）让自动检测工作

## 需要先读取的文件

1. `e:/guangdianbishe/ship_scraper_v10/scraper_v10.py` — 主要爬虫代码（2311行）
2. `e:/guangdianbishe/ship_labeling_new/manifests/all_candidates.csv` — 已整理数据清单
3. `e:/guangdianbishe/ship_scraper_v2/scripts/curate_dataset.py` — v2 种子/困难分类逻辑参考
4. `e:/guangdianbishe/organize_unlabeled.py` — 数据整理脚本（可复用）

## 最大风险

**修复后 maritimequest/wikimedia 候选仍与历史数据大量 SHA1 重复** → v10 仍然跑不出新数据。缓解方案：添加新搜索词、新数据源（google图片、shipspotting.com）、降低重复检测阈值。

## 项目文件布局

```
e:/guangdianbishe/
├── ship_scraper_v2/          # v2 参考实现（seed/hard策略原始代码）
├── ship_scraper_v3~v9.5/     # 历史版本（数据已整理）
├── ship_scraper_v10/         # 当前版本（已修复）
│   ├── scraper_v10.py        # 主爬虫 (2311 lines)
│   ├── output/
│   │   ├── processed/        # 353 张 (全与v9.5重复)
│   │   ├── raw/              # 原始下载
│   │   ├── reports/          # accepted.csv, source_candidates.csv
│   │   └── state/            # scraper_state.json
├── ship_labeling_new/        # ★ 待标注数据集
│   ├── seed/images/          # 294 张
│   ├── hard/images/          # 1656 张
│   └── manifests/            # CSV清单
├── organize_unlabeled.py     # 数据整理脚本
└── context/current/          # 项目上下文（本目录）
```

## 给下一位 AI 的续写提示

```
你现在接手 ship_scraper_v10 项目。当前代码已修复4个bug但未验证。

你的任务：
1. 先读 context/current/ 下的所有md文件
2. 运行10分钟烟雾测试验证修复效果
3. 根据测试结果决定：跑完整8小时任务 / 进一步调试
4. 关键判断标准：output/reports/accepted.csv 中是否有新 SHA1（与 ship_scraper_v9.5/output/reports/accepted.csv 对比）

不要做的事情：
- 不要改 trust_env 设置（中国网络环境需要代理）
- 不要重新整理 ship_labeling_new 数据
- 不要在没验证修复效果前就改 scraper 代码
```
